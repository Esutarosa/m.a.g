import type { FC } from 'react';

import { AdminContainer } from '@/components/layouts';

const AdminSiteSettings: FC = () => {
  return (
    <AdminContainer>
      AdminSiteSettings
    </AdminContainer>
  );
}

export default AdminSiteSettings;