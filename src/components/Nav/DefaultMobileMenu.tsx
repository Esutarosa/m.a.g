import type { FC } from 'react';

interface DefaultMobileMenuProps { }

const DefaultMobileMenu: FC<DefaultMobileMenuProps> = ({ }) => {
  return (
    <div>
      DefaultMobileMenu
    </div>
  );
}

export default DefaultMobileMenu;