import type { FC } from 'react';

import { Layout } from '@/components/layouts';

const SocialPage: FC = () => {
  return (
    <Layout>
      SocialPage
    </Layout>
  );
}

export default SocialPage;